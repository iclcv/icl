file(GLOB SOURCES src/ICLMarkers/*.cpp)
file(GLOB HEADERS src/ICLMarkers/*.h)

icl_add_module(ICLMarkers
  SOURCES ${SOURCES}
  HEADERS ${HEADERS}
  ICL_DEPS ICLGeom
  PKGCONFIG_DEPS ICLGeom-${SO_VERSION}
  HAS_DEMOS HAS_APPS)
